These files are used to test xbee connection.

There are two versions of xbee:
* use serial library.
* using XbeeDevice python library

Install library
* serial library:
pip install pyserial
* XbeeDevice library:
pip install digi-xbee

